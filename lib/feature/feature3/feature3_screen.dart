import 'package:flutter/material.dart';

class Feature3Screen extends StatelessWidget {
  const Feature3Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Feature 3'),
    );
  }
}
